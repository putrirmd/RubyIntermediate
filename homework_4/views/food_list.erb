<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Bee's</title>
    <link rel="stylesheet" href="<%= URI('style.css') %>">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/js/bootstrap.min.js"></script>
</head>

<body>
    <div class="container">
        <div class="row">
            <div class="col">
                <img src="<%= URI('header.jpg') %>" width=100%>
            </div>
        </div>
    </div>
    <div class="container" >
        <div class="row">
            <div class="col-lg-8 col-centered rounded" style="background-color: white;">
                <h1 class="title"><b>Bee's Items List</b></h1><br>
                <div class="table-responsive-sm table-wrapper-scroll-y my-custom-scrollbar">
                    <table class="table table-striped">
                        <thead>
                            <tr>
                                <th>Name</th>
                                <th>Price</th>
                                <th colspan="3" style="text-align: center;">Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                        <% items.each do |item| %>
                            <tr>
                                <td><%= item.name %></td>
                                <td>Rp <%= item.price %>,00</td>
                                <td><a href="/foods/detail/<%= item.id %>" class="btn btn-primary btn-sm">Show</a></td>
                                <td><a href="/foods/edit/<%= item.id %>" class="btn btn-primary btn-sm">Edit</a></td>
                                <td><a href="/foods/delete/<%= item.id %>" class="btn btn-primary btn-sm" >Delete</a></td>
                            </tr>
                        <% end %>
                        </tbody>
                    </table>
                </div>
                <div class="row">
                    <div class="col-lg-12 col-centered rounded" style="height: 50px;">
                        <a href="/foods/create" class="btn btn-primary btn-sm" style="margin-top:7px;">Add</a>
                    </div>
                </div>
                <div class="row">
                    <div class="col-lg-12 col-centered rounded" style="height:50px;">
                        <a href="/" class="btn btn-primary btn-sm">Back</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
</html>