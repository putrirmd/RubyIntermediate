<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Bee's</title>
    <link rel="stylesheet" href="<%= url('style.css') %>">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
</head>

<body>
    <div class="container">
        <div class="row">
            <div class="col">
                <img src="<%= url('header.jpg') %>" width=100%>
            </div>
        </div>
    </div>
    <div class="container" >
        <div class="row">
            <div class="col-lg-8 col-centered rounded" style="background-color: white;">
                <h1 class="title"><b>Create New Menu</b></h1><br>
                <div>
                    <form action="/foods/create" method="POST">
                        <label for="fname">Food Name</label>
                        <input type="text" id="fname" name="foodname" placeholder="Item name..">
                    
                        <label for="fprice">Food Price</label>
                        <input type="text" id="fprice" name="foodprice" placeholder="Item price..">
                    
                        <label for="fcategory">Categories</label>
                        <select id="fcategory" name="foodcategory">
                        <% categories.each do |category| %>
                            <option value="<%= category.id %>"><%= category.name %></option>
                        <% end %>
                        </select>
                        <input type="submit" value="Submit">
                    </form>
                </div>
                <div class="row">
                    <div class="col-lg-12 col-centered rounded" style="height:70px;">
                        <a href="/foods" class="btn btn-primary btn-sm">Back</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
</html>